import React, {useEffect, useState, useRef} from 'react';

import {
  View,
  StyleSheet,
  Text,
  SafeAreaView,
  Image,
  ScrollView,
  Dimensions,
  Animated,
} from 'react-native';


const UserDesign = ({navigation, route}: any) => {
    return (
        <ScrollView></ScrollView>
    )
}

export default UserDesign;